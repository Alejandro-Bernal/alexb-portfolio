# Browser-facing API URL (baked into the JS bundle at image build time).
# For Docker, the browser still needs a host-reachable URL (e.g. localhost).
VITE_BACKEND_API_URL=http://localhost:8000/contact
VITE_API_KEY=your-api-key-here

# Host port for the portfolio UI
PORTFOLIO_PORT=3000
