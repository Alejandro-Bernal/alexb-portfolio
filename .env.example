# Used by local `npm run dev` and by Docker image builds (via BuildKit secret).
# Vite bakes VITE_* values into the client JS at build time.
VITE_BACKEND_API_URL=http://localhost:8000/contact
VITE_API_KEY=your-api-key-here

# Host port for the portfolio UI (docker compose only)
PORTFOLIO_PORT=3000
